{% extends "AppBundle:Layouts:layout.html.twig" %}
{% block content %}
    <div class="col-lg-12 box-default">
        <div class="row">
            {{dump(cuenta)}}
            <a href="" class="btn btn-info">Depositar</a>
        </div>
            <div>
                {{form_start(form)}}
                {{form_end(form)}}
            </div>

    </div>
{% endblock %}
